# <div align="center"><strong>⚙️</strong></div>
# <div align="center"><strong>МОДУЛЬ 1</strong></div>
# <div align="center"><strong>КОД 09.02.06-1-2026</strong></div>

<br/>

> [!WARNING]
> Файл собран под задание **Модуля 1** из PDF **«КОД 09.02.06-1-2026 Том 1»**.  
> Последовательность настройки:
> 1. Таблица масок и IP-адресов  
> 2. Для каждого устройства: преднастройка → имя → IP-настройка  
> 3. Интернет на все устройства сразу  
> 4. Остальные задания по порядку  
> 5. Проверка после каждого задания  

---

## Таблица масок всех подсетей

| Префикс | Маска подсети | Количество хостов |
|---|---|---:|
| /27 | 255.255.255.224 | 30 |
| /28 | 255.255.255.240 | 14 |
| /29 | 255.255.255.248 | 6 |
| /30 | 255.255.255.252 | 2 |

---

## Таблица подсетей

| Сеть | Подсеть | Назначение |
|---|---|---|
| ISP-HQ | 172.16.1.0/28 | Канал между ISP и HQ-RTR |
| ISP-BR | 172.16.2.0/28 | Канал между ISP и BR-RTR |
| TUN | 172.16.0.0/30 | GRE-туннель между HQ-RTR и BR-RTR |
| VLAN 100 | 192.168.100.0/27 | Сегмент HQ-SRV |
| VLAN 200 | 192.168.200.0/27 | Сегмент HQ-CLI |
| BR-Net | 192.168.0.0/28 | Сегмент BR-SRV |

---

## Таблица IP-адресов устройств

| Устройство | Интерфейс | IP-адрес | Префикс | Шлюз |
|---|---|---|---|---|
| ISP | ens192 | DHCP | DHCP | DHCP |
| ISP | ens224 | 172.16.1.1 | /28 | — |
| ISP | ens256 | 172.16.2.1 | /28 | — |
| HQ-RTR | ens192 | 172.16.1.2 | /28 | 172.16.1.1 |
| HQ-RTR | ens224.100 | 192.168.100.1 | /27 | — |
| HQ-RTR | ens224.200 | 192.168.200.1 | /27 | — |
| HQ-RTR | gre1 | 172.16.0.1 | /30 | — |
| BR-RTR | ens192 | 172.16.2.2 | /28 | 172.16.2.1 |
| BR-RTR | ens224 | 192.168.0.1 | /28 | — |
| BR-RTR | gre1 | 172.16.0.2 | /30 | — |
| HQ-SRV | ens192 | 192.168.100.2 | /27 | 192.168.100.1 |
| BR-SRV | ens192 | 192.168.0.2 | /28 | 192.168.0.1 |
| HQ-CLI | ens192 | DHCP | /27 | 192.168.200.1 |

---

# ✔️ Устройство 1 — ISP

## Преднастройка
Проверяем репозитории:
```bash
nano /etc/apt/sources.list
apt update
```

Включаем маршрутизацию:
```bash
echo net.ipv4.ip_forward=1 > /etc/sysctl.conf
sysctl -p
```

## Настройка имени
```bash
hostnamectl set-hostname isp.au-team.irpo
bash
```

## Настройка IP
```bash
nano /etc/network/interfaces
```

```bash
auto ens192
iface ens192 inet dhcp

auto ens224
iface ens224 inet static
    address 172.16.1.1/28

auto ens256
iface ens256 inet static
    address 172.16.2.1/28
```

```bash
systemctl restart networking
```

## Проверка
```bash
hostnamectl
cat /proc/sys/net/ipv4/ip_forward
ip a
ip route
ping -c 4 172.16.1.2
ping -c 4 172.16.2.2
```

---

# ✔️ Устройство 2 — HQ-RTR

## Преднастройка
Проверяем репозитории:
```bash
nano /etc/apt/sources.list
apt update
```

Если мешает NetworkManager:
```bash
systemctl stop NetworkManager
systemctl disable NetworkManager
systemctl mask NetworkManager
```

Включаем маршрутизацию:
```bash
echo net.ipv4.ip_forward=1 > /etc/sysctl.conf
sysctl -p
```

Устанавливаем поддержку VLAN:
```bash
apt install vlan -y
echo 8021q >> /etc/modules
modprobe 8021q
```

Устанавливаем поддержку GRE:
```bash
echo ip_gre >> /etc/modules
modprobe ip_gre
```

## Настройка имени
```bash
hostnamectl set-hostname hq-rtr.au-team.irpo
bash
```

## Настройка IP, VLAN и GRE
```bash
nano /etc/network/interfaces
```

```bash
auto ens192
iface ens192 inet static
    address 172.16.1.2/28
    gateway 172.16.1.1

auto ens224
iface ens224 inet manual

auto ens224.100
iface ens224.100 inet static
    address 192.168.100.1/27
    vlan-raw-device ens224

auto ens224.200
iface ens224.200 inet static
    address 192.168.200.1/27
    vlan-raw-device ens224

auto gre1
iface gre1 inet tunnel
    address 172.16.0.1
    netmask 255.255.255.252
    mode gre
    local 172.16.1.2
    endpoint 172.16.2.2
    ttl 64
```

```bash
systemctl restart networking
```

## Проверка
```bash
hostnamectl
cat /proc/sys/net/ipv4/ip_forward
lsmod | grep 8021q
lsmod | grep gre
ip a
ip route
ping -c 4 172.16.1.1
ping -c 4 172.16.0.2
```

---

# ✔️ Устройство 3 — BR-RTR

## Преднастройка
Проверяем репозитории:
```bash
nano /etc/apt/sources.list
apt update
```

Если мешает NetworkManager:
```bash
systemctl stop NetworkManager
systemctl disable NetworkManager
systemctl mask NetworkManager
```

Включаем маршрутизацию:
```bash
echo net.ipv4.ip_forward=1 > /etc/sysctl.conf
sysctl -p
```

Устанавливаем поддержку GRE:
```bash
echo ip_gre >> /etc/modules
modprobe ip_gre
```

## Настройка имени
```bash
hostnamectl set-hostname br-rtr.au-team.irpo
bash
```

## Настройка IP и GRE
```bash
nano /etc/network/interfaces
```

```bash
auto ens192
iface ens192 inet static
    address 172.16.2.2/28
    gateway 172.16.2.1

auto ens224
iface ens224 inet static
    address 192.168.0.1/28

auto gre1
iface gre1 inet tunnel
    address 172.16.0.2
    netmask 255.255.255.252
    mode gre
    local 172.16.2.2
    endpoint 172.16.1.2
    ttl 64
```

```bash
systemctl restart networking
```

## Проверка
```bash
hostnamectl
cat /proc/sys/net/ipv4/ip_forward
lsmod | grep gre
ip a
ip route
ping -c 4 172.16.2.1
ping -c 4 172.16.0.1
```

---

# ✔️ Устройство 4 — HQ-SRV

## Преднастройка
Проверяем репозитории:
```bash
nano /etc/apt/sources.list
apt update
```

Временный DNS до настройки собственного DNS:
```bash
nano /etc/resolv.conf
```

```bash
nameserver 77.88.8.8
nameserver 1.1.1.1
```

## Настройка имени
```bash
hostnamectl set-hostname hq-srv.au-team.irpo
bash
```

## Настройка IP
```bash
nano /etc/network/interfaces
```

```bash
auto ens192
iface ens192 inet static
    address 192.168.100.2/27
    gateway 192.168.100.1
```

```bash
systemctl restart networking
```

## Проверка
```bash
hostnamectl
ip a
ip route
ping -c 4 192.168.100.1
ping -c 4 77.88.8.8
```

---

# ✔️ Устройство 5 — BR-SRV

## Преднастройка
Проверяем репозитории:
```bash
nano /etc/apt/sources.list
apt update
```

Временный DNS:
```bash
nano /etc/resolv.conf
```

```bash
nameserver 77.88.8.8
nameserver 1.1.1.1
```

## Настройка имени
```bash
hostnamectl set-hostname br-srv.au-team.irpo
bash
```

## Настройка IP
```bash
nano /etc/network/interfaces
```

```bash
auto ens192
iface ens192 inet static
    address 192.168.0.2/28
    gateway 192.168.0.1
```

```bash
systemctl restart networking
```

## Проверка
```bash
hostnamectl
ip a
ip route
ping -c 4 192.168.0.1
ping -c 4 77.88.8.8
```

---

# ✔️ Устройство 6 — HQ-CLI

## Преднастройка
Проверяем репозитории:
```bash
nano /etc/apt/sources.list
apt update
```

Временный DNS:
```bash
nano /etc/resolv.conf
```

```bash
nameserver 77.88.8.8
nameserver 1.1.1.1
```

## Настройка имени
```bash
hostnamectl set-hostname hq-cli.au-team.irpo
bash
```

## Настройка IP
На данном этапе IP вручную не задаётся, так как по заданию HQ-CLI должен получать адрес по DHCP от HQ-RTR.

## Проверка
```bash
hostnamectl
```

---

# ✔️ Интернет на все устройства сразу

## NAT на ISP
```bash
apt install iptables iptables-persistent -y

iptables -t nat -A POSTROUTING -s 172.16.1.0/28 -o ens192 -j MASQUERADE
iptables -t nat -A POSTROUTING -s 172.16.2.0/28 -o ens192 -j MASQUERADE

netfilter-persistent save
systemctl restart netfilter-persistent
```

## NAT на HQ-RTR
```bash
apt install iptables iptables-persistent -y

iptables -t nat -A POSTROUTING -s 192.168.100.0/27 -o ens192 -j MASQUERADE
iptables -t nat -A POSTROUTING -s 192.168.200.0/27 -o ens192 -j MASQUERADE

netfilter-persistent save
systemctl restart netfilter-persistent
```

## NAT на BR-RTR
```bash
apt install iptables iptables-persistent -y

iptables -t nat -A POSTROUTING -s 192.168.0.0/28 -o ens192 -j MASQUERADE

netfilter-persistent save
systemctl restart netfilter-persistent
```

## Проверка
На ISP:
```bash
iptables -t nat -L -n -v
```

На HQ-RTR:
```bash
iptables -t nat -L -n -v
ping -c 4 8.8.8.8
```

На BR-RTR:
```bash
iptables -t nat -L -n -v
ping -c 4 8.8.8.8
```

На HQ-SRV:
```bash
ping -c 4 8.8.8.8
```

На BR-SRV:
```bash
ping -c 4 8.8.8.8
```

---

# ✔️ Задание 3 — Создание пользователей

## HQ-SRV и BR-SRV
```bash
useradd -m -u 2026 -s /bin/bash sshuser
passwd sshuser
usermod -aG sudo sshuser
echo 'sshuser ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers
```

## HQ-RTR и BR-RTR
```bash
useradd -m -s /bin/bash net_admin
passwd net_admin
usermod -aG sudo net_admin
echo 'net_admin ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers
```

## Проверка
```bash
id sshuser
id net_admin
```

```bash
su - sshuser
sudo whoami
```

```bash
su - net_admin
sudo whoami
```

Ожидаемый результат:
```bash
root
```

---

# ✔️ Задание 4 — VLAN в сегменте HQ

VLAN-интерфейсы уже настроены на этапе настройки `HQ-RTR`.

## Проверка
```bash
ip a
```

Должны отображаться:
- `ens224.100`
- `ens224.200`

Проверка доступности HQ-SRV:
```bash
ping -c 4 192.168.100.2
```

---

# ✔️ Задание 5 — SSH на HQ-SRV и BR-SRV

```bash
apt install openssh-server -y
nano /etc/ssh/sshd_config
```

Добавляем или изменяем:
```bash
Port 2026
PermitRootLogin no
MaxAuthTries 2
AllowUsers sshuser
Banner /etc/ssh/banner
PasswordAuthentication yes
```

```bash
echo 'Authorized access only' > /etc/ssh/banner
systemctl restart ssh
```

## Проверка
```bash
ss -tulnp | grep 2026
```

```bash
ssh -p 2026 sshuser@192.168.100.2
ssh -p 2026 sshuser@192.168.0.2
```

---

# ✔️ Задание 6 — GRE-туннель

GRE уже настроен на этапе настройки роутеров.

## Проверка на HQ-RTR
```bash
ip a show gre1
ping -c 4 172.16.0.2
```

## Проверка на BR-RTR
```bash
ip a show gre1
ping -c 4 172.16.0.1
```

---

# ✔️ Задание 7 — Динамическая маршрутизация OSPF

## На HQ-RTR и BR-RTR
```bash
apt install frr -y
nano /etc/frr/daemons
```

Включаем OSPF:
```bash
ospfd=yes
```

```bash
systemctl enable --now frr
systemctl restart frr
```

## HQ-RTR
```bash
vtysh
```

```bash
conf t
router ospf
 router-id 1.1.1.1
 passive-interface default
 network 172.16.0.0/30 area 0
 network 192.168.100.0/27 area 1
 network 192.168.200.0/27 area 2
area 0 authentication
exit
interface gre1
 no ip ospf passive
 ip ospf authentication
 ip ospf authentication-key P@ssw0rd
exit
write
```

## BR-RTR
```bash
vtysh
```

```bash
conf t
router ospf
 router-id 2.2.2.2
 passive-interface default
 network 172.16.0.0/30 area 0
 network 192.168.0.0/28 area 3
area 0 authentication
exit
interface gre1
 no ip ospf passive
 ip ospf authentication
 ip ospf authentication-key P@ssw0rd
exit
write
```

## Проверка
На обоих роутерах:
```bash
vtysh -c "show ip ospf neighbor"
vtysh -c "show ip route ospf"
```

Проверка маршрутизации между офисами:
```bash
ping -c 4 192.168.0.2
ping -c 4 192.168.100.2
```

---

# ✔️ Задание 8 — DHCP для HQ-CLI

На HQ-RTR:
```bash
apt install isc-dhcp-server -y
nano /etc/dhcp/dhcpd.conf
```

```bash
subnet 192.168.200.0 netmask 255.255.255.224 {
  range 192.168.200.2 192.168.200.30;
  option routers 192.168.200.1;
  option domain-name-servers 192.168.100.2;
  option domain-name "au-team.irpo";
  default-lease-time 600;
  max-lease-time 7200;
}
```

```bash
nano /etc/default/isc-dhcp-server
```

```bash
INTERFACESv4="ens224.200"
```

```bash
systemctl enable --now isc-dhcp-server
```

## Проверка
На HQ-RTR:
```bash
systemctl status isc-dhcp-server
cat /var/lib/dhcp/dhcpd.leases
```

На HQ-CLI:
```bash
ip a
ip route
cat /etc/resolv.conf
ping -c 4 192.168.200.1
ping -c 4 192.168.100.2
ping -c 4 8.8.8.8
```

---

# ✔️ Задание 9 — DNS на HQ-SRV

На HQ-SRV:
```bash
apt install bind9 bind9-utils -y
```

## `/etc/bind/named.conf.options`
```bash
nano /etc/bind/named.conf.options
```

```bash
options {
    directory "/var/cache/bind";

    forwarders {
        77.88.8.8;
        77.88.8.3;
    };

    dnssec-validation auto;
    listen-on { any; };
    allow-query { any; };
    recursion yes;
};
```

## `/etc/bind/named.conf.local`
```bash
nano /etc/bind/named.conf.local
```

```bash
zone "au-team.irpo" {
    type master;
    file "/etc/bind/db.au-team.irpo";
};

zone "100.168.192.in-addr.arpa" {
    type master;
    file "/etc/bind/db.192.168.100";
};

zone "200.168.192.in-addr.arpa" {
    type master;
    file "/etc/bind/db.192.168.200";
};
```

## `/etc/bind/db.au-team.irpo`
```bash
nano /etc/bind/db.au-team.irpo
```

```bash
$TTL 1D
@   IN SOA hq-srv.au-team.irpo. root.au-team.irpo. (
        2026010101
        12H
        1H
        1W
        1H )

@       IN NS      hq-srv.au-team.irpo.
hq-rtr  IN A       192.168.100.1
br-rtr  IN A       192.168.0.1
hq-srv  IN A       192.168.100.2
hq-cli  IN A       192.168.200.2
br-srv  IN A       192.168.0.2
docker  IN A       172.16.1.1
web     IN A       172.16.2.1
```

## `/etc/bind/db.192.168.100`
```bash
nano /etc/bind/db.192.168.100
```

```bash
$TTL 1D
@   IN SOA hq-srv.au-team.irpo. root.au-team.irpo. (
        2026010101
        12H
        1H
        1W
        1H )

@       IN NS      hq-srv.au-team.irpo.
1       IN PTR     hq-rtr.au-team.irpo.
2       IN PTR     hq-srv.au-team.irpo.
```

## `/etc/bind/db.192.168.200`
```bash
nano /etc/bind/db.192.168.200
```

```bash
$TTL 1D
@   IN SOA hq-srv.au-team.irpo. root.au-team.irpo. (
        2026010101
        12H
        1H
        1W
        1H )

@       IN NS      hq-srv.au-team.irpo.
2       IN PTR     hq-cli.au-team.irpo.
```

Проверка и запуск:
```bash
named-checkconf
named-checkzone au-team.irpo /etc/bind/db.au-team.irpo
systemctl restart bind9
systemctl enable bind9
```

На всех машинах после настройки DNS:
```bash
nano /etc/resolv.conf
```

```bash
search au-team.irpo
nameserver 192.168.100.2
```

## Проверка
```bash
nslookup hq-srv.au-team.irpo
nslookup hq-cli.au-team.irpo
nslookup docker.au-team.irpo
nslookup web.au-team.irpo
nslookup 192.168.100.2
nslookup 192.168.200.2
```

---

# ✔️ Задание 10 — Часовой пояс

На всех устройствах:
```bash
timedatectl set-timezone Asia/Tomsk
```

## Проверка
```bash
timedatectl
```

---

# ✔️ Итоговая проверка модуля

## Проверка адресации
На всех устройствах:
```bash
ip a
ip route
```

## Проверка интернета
На HQ-SRV, BR-SRV, HQ-RTR, BR-RTR:
```bash
ping -c 4 8.8.8.8
```

## Проверка туннеля
На HQ-RTR:
```bash
ping -c 4 172.16.0.2
```

На BR-RTR:
```bash
ping -c 4 172.16.0.1
```

## Проверка маршрутизации между офисами
На BR-SRV:
```bash
ping -c 4 192.168.100.2
```

На HQ-SRV:
```bash
ping -c 4 192.168.0.2
```

## Проверка DHCP
На HQ-CLI:
```bash
ip a
ip route
cat /etc/resolv.conf
```

## Проверка DNS
```bash
nslookup hq-srv.au-team.irpo
nslookup hq-cli.au-team.irpo
nslookup docker.au-team.irpo
nslookup web.au-team.irpo
```

## Проверка SSH
```bash
ssh -p 2026 sshuser@192.168.100.2
ssh -p 2026 sshuser@192.168.0.2
```
